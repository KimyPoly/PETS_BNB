
<%= simple_form_for [@booking, @review] do |f| %>
    <%= f.input :description, placeholder: 'Laissez un commentaire...' %>
    <%= f.input :rating, collection: Review::AUTHORIZED_RATINGS %>
    <%= f.submit class: 'btn btn-flat' %>
<% end %>

