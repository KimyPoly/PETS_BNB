<% end %<ul>
  <% @bookings.each do |booking|%>
    <li><%= link_to booking.name, booking_path(booking) %></li>
  <% end %>
</ul>

<a class = "btn btn-primary" href = "pets/pets[:id]/bookings/new" >New booking</a>
